# 会话交互修正验收（2026-09-12）

## 当前契约

- Web、移动 Web、iOS 连续工具活动合并为默认收起的执行活动组，以无背景、无边框的图标/摘要/箭头信息行展示，展开条目同样无卡片且不额外留行距；所有活动的实际内容仅在末级展开显示，中间层只有图标、摘要、下拉箭头，移除结构化详情引导；消息、错误与图片结果保留在外层。
- 结果附件只展示文件名，点击文件名预览。消息正文中的已识别本地 Markdown 链接不暴露原始地址；复制原文仍保留完整消息。
- 图片在消息正文上方横向排列为 88×88 缩略图，用户图片右对齐，多图可横向滑动；点击读取的完整图片进入大图预览。Web 可切换原始尺寸，iOS 图片使用透明全屏蒙层预览：点击图片外蒙层或未放大时下滑关闭，支持双指缩放及双击放大/还原；放大后拖动仅浏览图片。文件继续使用 Quick Look。原图字节保留，不将缩略图重新编码为原图。支持会话明确引用的本地图片、imageView、结构化工具内联图片。Web 文本和 PDF 可直接预览，其他格式显示不支持预览；iOS 使用系统 Quick Look 和分享。
- 项目优先使用 Codex 保存的归属与项目根目录，Git common directory 用于识别 worktree。保留会话实际 cwd，不改写 Codex 数据库。
- 移动 Web/iOS 的“我的”上方设备卡片统一展示远程待机、远程控制状态，控制授权仍由本机管理。
- iOS 使用 NavigationStack 项目和会话导航，保留系统返回按钮和边缘返回手势；窗口空白点击收起键盘，输入控件不触发此手势，已有滚动收键盘和下拉刷新继续保留。
- iOS 项目内会话取消状态分类，服务端按更新时间倒序。待处理仍进入动态和角标。
- 对话显示已加载轮次提供的执行时长，当前轮次依据原生开始时间计算；不是任务创建后的自然经过时间。

## 风险与恢复

R2：新增读取接口 `/api/threads/{id}/artifacts/{hash}`（以及需 parentId 的临时聊天接口）。复用现有绑定、只读授权与桥接代次校验；只接受历史中明确引用的附件，不接受调用者传入路径。限普通文件及 8 MiB，拒绝最终路径符号链接。Web 预览以文本节点或 sandbox PDF frame 展示，不执行附件 HTML。未增加依赖、数据库写入或生产配置。

恢复方式：恢复本次后端投影/附件路由与对应客户端版本即可，无数据迁移；不要覆盖并行任务修改的 CLI、README、MULTI_CLOUD 文档或既有 iOS 工程。尚未提交、推送或部署。

## 已执行验证

- Python 全套 150 项通过（包含同时存在的其他任务测试）；随后新增的附件云端只读/关闭桥接验证通过。附件专项共 3 项通过。
- Node 客户端测试 15 项通过；Web JavaScript 语法检查与 git diff --check 通过。
- Swift Core 测试 8 项通过；iOS iphoneos target 完整未签名 App 构建通过，包括文件名点击预览修正。
- 浏览器固定数据渲染验证：执行活动默认整组收起，图片元素加载，390px 宽度下仅显示文件名，点击文件名显示文本预览；另核验桌面展开条目无背景、无边框。该验证不是生产云端完整链路验收。
- 本机实际 Codex 项目元数据结构已核对；临时 Git 仓库/worktree 测试证明不同 cwd 归入同一项目。

## 未完成门禁

独立审查 Agent 未能运行：服务返回模型使用额度限制，未形成独立结论。按照工程规范，不将实施者自检作为独立审查。

iOS 真机边缘返回、空白点击、系统 Quick Look、后台恢复，以及真实云端多端附件链路需进一步验收。未签名构建不等同于真机交互验证。R2 独立审查及人类验收仍待完成。

## iOS 图片蒙层预览补充验证

2026-09-12：图片切换到 `ImageLightbox.swift` 中的 UIKit overFullScreen 查看器，保留会话作为蒙层底部背景。使用 UIScrollView 处理缩放；纵向关闭手势仅在适配尺寸且单指向下时启用。取消拖动会回弹，关闭可用点击蒙层、右上角按钮或 VoiceOver 退出手势。尊重系统减少动态效果设置。

独立构建目录 `/tmp/carryon-lightbox-products` 的 iphoneos arm64 未签名完整 App 构建通过；无已启动模拟器，真机缩放/手势和视觉体验仍需验收。当前只修改 iOS 图片预览及对应说明，文件 Quick Look 未替换。

## 工作区抽屉与连接申请（2026-09-12）

移动 Web/iOS：连接说明与设备移除移入工作区抽屉；无标题/完成文字，左上连接帮助；当前工作区绿色“当前使用”，其他在线/离线。整行左滑展开右侧移除/详情，保留左侧名称与状态，长名称省略，无更多按钮。移除二次确认，名称和选择以目标设备 ID 为准，移除其他设备不切换当前设备。详情显示设备实际 status.deviceInfo 主机名与监听地址/端口，旧服务或离线明确不可用。连接步骤区分 CLI/桌面；命令及地址右上复制图标，成功变勾 1.8 秒后恢复。

我的连接申请有待处理时显示红色数字徽标；申请页展示历史结果和时间，清空有确认，仅影响历史。云端以 link-history.json 保存最近 1000 条已同意/拒绝/过期记录，无轮询 secret、凭证或确认码。损坏历史降级提示，不阻止新申请；写历史失败不改变已完成授权。旧请求响应不能覆盖更新后的列表。旧版未保存的历史无法回溯。

风险/恢复：申请授权和设备凭证契约保持原有来源；新增历史是展示投影。回退客户端/服务代码时保留 link-history.json 即可，无需改设备注册记录；没有迁移或操作真实设备。检查过损坏文件、字段白名单、上限、过期只记录一次、清空保留 pending/设备/授权结果、未登录及跨源清空拒绝、旧快照晚到丢弃。

验证：16 项相关 Node 测试、22 项 Python 历史/绑定/控制台测试通过，iOS 无签名 iphoneos 构建通过。390×844 隔离浏览器预览已检查整行左滑、名称保留、右侧操作、绿色状态、红色徽标、历史结果、复制图标勾选与恢复。预览使用模拟设备和记录，未删除真实设备或清空真实历史；iOS 真机手势与真实云端往返待验收。

最终文案：移除我的页的本机权限说明和连接申请页的自动登记说明；历史标题同行右侧显示清空图标，历史标题及记录使用淡色小字。独立复审原阻塞已修复；旧成功及失败请求均进行版本过滤。
