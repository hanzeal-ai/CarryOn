# iOS 动态与已读保留

动态页请求 `/api/activity?includeRead=true`，保留已读项并置灰，仍可打开原会话。项目标签使用后端 `projectName`；旧服务响应缺少该字段时仅显示项目路径的最后一级，无项目显示「最近」。

「清空已读」调用 `POST /api/notifications/clear-read`，按云端验证的账号身份及工作区推进已读动态的清理游标。身份从登录会话经网关传给本机，不信任请求体中的身份字段；同一绑定中的不同账号也各自清理。不会删除会话、消息、通知事件或其他账号的记录。既有已读判断仍复用绑定读游标与 Codex 原生已读同步。清理不处理审批，也不停止任务；待处理事项仍独立计数。新事件序号超过清理游标后，该会话重新出现在动态中。默认 `/api/activity` 仍供已有客户端和待处理角标使用，已读历史不增加角标。

此功能需云端网关、本机 CLI 与 iOS 配套升级；旧网关不传递账号清理身份，远程清空请求会返回 409 提示升级，不退回共享清理。

完成时间由原生已完成轮次的开始时间与耗时计算，返回 Unix 秒字段 `completedAt`。iOS 在会话轮次及会话列表显示本地时间；缺少原生时间的记录不使用文件修改时间或当前时间代替。

## 验证与恢复

变更为本地代码实现，不含部署或真实数据清理。SQLite 增加 `activity_cleared` 表，既有读游标与通知事件保持不变；回退代码可忽略新表，无需删除用户数据。

相关测试覆盖已读保留、同绑定不同账号隔离、HTTP 身份伪造拒绝、持久化恢复、新事件重现、清理期间未读保留、原生已读同步、只读连接清理、待审批状态保留及原生完成时间。验证日志：`/tmp/carryon-activity-tests-final.log`、`/tmp/carryon-activity-transport.log`、`/tmp/carryon-activity-build.log`。尚未完成真机登录后的视觉与点击验收。
