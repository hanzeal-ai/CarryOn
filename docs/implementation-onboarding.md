# 初始化与工作区实施记录

2026-09-14。当前任务授权本地实现和验证，不含提交、推送、部署、真实账号/数据迁移。

## 当前目标

首次 CLI init / 桌面向导默认连接 Codex App，建立默认工作区，iOS 已登录账号扫码确认后默认自动启动；可取消自动启动。重复初始化续办并保留数据与绑定。日常 start/status/stop 复用配置。iOS 保留当前 Design、导航、工作区切换及扫码交互风格。

多用户账号和每工作区成员授权为权威后端规则；设备凭证与账号身份分离。新增工作区先配置再分配，已有身份无需再次扫码，新身份邀请双向确认。独立 app-server 需验证鉴权复用、独立会话存储及真实运行限制，未验证不宣称支持。用户已确认本次采用账号密码自助注册，保留云端地址输入；不接入邮箱验证码。公共托管地址与账号恢复方式仍未定。

## 风险与验证

R2：跨客户端生命周期、身份与授权。复用服务及配置契约。验证应覆盖重复/取消/超时/部分成功、未登录/跨账号/撤权、HTTP/实时/文件/推送全入口。恢复保留原有私有目录、Token 和请求日志，停止本次服务后可回到原代码；不自动迁移真实云端数据。独立审查必须能否决实现。

## 初始基线

工作树已有会话/子代理/iOS/UI等未提交改动，不覆盖。CLI 没有 init；start 仅在新启动且已有云绑定时尝试桥接。云端是单管理员，旧扫码用于手机登录而非工作区绑定。控制会话不持久化。未启动模拟器。

## 当前交付状态

- [x] 共享初始化进度、保存启动参数；后台桥接等待与重连，显式关闭取消重连。
- [x] 账号密码注册、持久化账号会话、工作区成员权限与短时扫码邀请；权限在 HTTP、实时、文件、推送入口校验。
- [x] CLI init 扫码/已确认账号绑定、续办、过期恢复、默认自动启动与仅保存选项。
- [x] 桌面初始化共用 CLI 引擎；服务停止时仍显示保存的绑定。
- [x] 当前 iOS 登录布局增加注册、确认密码；工作区扫码确认展示账号及权限。
- [x] 现有工作区成员授权、撤销、新账号双向确认；桌面与 CLI 可复用同云端其他工作区已确认账号。
- [ ] 独立 app-server 工作区完整适配及可执行的运行限制。
- [ ] 控制会话选择的持久化及启动校验；新工作区暂不分配流程、工作区自动启动策略与多工作区调度。
- [ ] 真机 HTTPS 扫码/撤权、APNs 实际投递、干净机器安装及完整 UI 操作验收。

### 已执行验证

- 全量 Python：347 tests，1 skipped，其余通过；`/tmp/carryon-onboarding-python-final.log`。此后增加离线读取和自动重连覆盖，相关 36 项通过：`/tmp/carryon-onboarding-last.log`。
- 最后增加已有账号来源撤权后不能再授权的测试，onboarding 29 项通过：`/tmp/carryon-onboarding-member-final.log`。
- JavaScript 36 项、Swift package 37 项通过：`/tmp/carryon-onboarding-js.log`、`/tmp/carryon-onboarding-swift-tests.log`。
- iOS Simulator Debug build 通过：`/tmp/carryon-onboarding-build-final.log`；最终桌面 Swift 类型检查通过：`/tmp/carryon-desktop-member-final.log`。
- 模拟器已安装和启动，观察到注册界面及确认密码字段。截图 `/tmp/carryon-onboarding-login.png`；CUA 滚动/点击失败，不能视为完整交互验收。未提交真实注册。
- GPT-5.5 独立复审有条件接受初始化/账号/成员权限切片；匿名绑定限速、撤权时订阅释放、写入竞态不确定结果和已确认账号幂等记录容量问题已修复并复核。审查不覆盖独立 app-server、生产和真机交互。

### 独立 app-server 检查点

隔离临时 CODEX_HOME、复用本机文件式鉴权的无模型调用探测已成功：account/read 返回 ChatGPT 账号，thread/start 路径位于隔离目录，原数据库未收录探测会话，原 auth.json 修改时间未变。探测临时目录已清理。未验证 Keychain 鉴权、实际模型执行后的会话持久化、完整操作适配和目录/网络/审批隔离；没有把它开放成可选后端。

下一实施切片应先建立 app-server 完整协议适配及隔离验证，再接入新工作区配置与启动策略；不能用成员权限代替宿主机执行限制。

### 恢复与工作树边界

未提交、未部署、未迁移真实账号。恢复要求见 `ONBOARDING.md`；含多账号授权的设备注册表不能直接交给旧单管理员程序公开运行。本任务保留已有会话/子代理/UI 修改；并发出现的 `iOS/CarryOn/App/CloudSettingsView.swift` 未改动。
