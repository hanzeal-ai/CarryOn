# 2026-09-15 发布审查

用户授权提交、推送和部署当前源码。范围为 iOS 会话呈现、交互、性能与连接恢复，Codex 账号额度读取，以及项目内新建任务。设计探索压缩包、output 产物及只读探测脚本不在发布范围。

## 独立审查

GPT-5.6 Sol 基于 9e94c5d 到 be2604c 原始差异以及修复 1731e00 形成结论：Accept，无剩余发布阻断。

项目创建发现的两项问题已修复：原生发送前持锁再次核对项目/会话归属；保存 Git 状态并在结果确认时精确核对 worktree/local，拒绝 startingState。Git 状态读取有 3 秒超时，目录不可用或查询失败时拒绝创建。

此前额度接口、WebSocket 恢复审查及验证记录见同目录对应报告。本次按用户要求不重复运行本地测试；新增归属变化与环境错误断言由现有 GitHub 流水线执行。非 Git 结果确认分支尚无单独新增断言，为非阻断覆盖缺口。iOS 真机验收由用户自行进行。

## 部署与恢复

当前开发分支基准与 origin/main 均为 9e94c5d，公开 healthz 确认线上相同 release。通过快进更新 main 触发现有 gateway.yml，流水线自动检查、构建 wheel、部署并核对 release。接收器保留 previous release，激活失败自动回退；不改变账号、设备登记、请求日志、系统服务配置或代理配置。

云端流水线发布网关代码；iOS 源码随提交推送，不代表自动安装到手机。线上结果以 GitHub 部署步骤和 healthz 的最终提交号为准。
