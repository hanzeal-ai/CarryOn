# 当前流程与兼容边界审查

## 范围与结果

按 R2 检查 Python 服务/CLI、macOS 桌面、iOS、Web、协议文档及相关测试。以本次开始时的暂存版本为基线，保留此前改动。未提交、部署、修改生产配置或迁移真实账号/设备数据。

普通连接统一为电脑初始化、选择权限、手机扫码确认；默认云端不要求用户输入地址。自托管及多云端是仍受支持的能力，不作为遗留功能删除。

| 已处理项 | 原问题 | 当前行为 |
| --- | --- | --- |
| iOS / 移动 Web 指引 | 普通用户仍需复制云端地址，Web 推荐 start/bridge/connect 多步流程 | 默认 `carryon init`；自托管才显示与当前服务匹配的命令，包含路径前缀和 shell 转义 |
| CLI 初始化 | 首次必须手填地址，显式新云可能复用已有 bound 状态 | 后端提供默认值，保留显式自托管地址；按目标选择绑定，进行中的绑定拒绝切换目标 |
| 桌面连接 | 普通入口展示地址，另一个窗口仍走旧连接申请，要求先启动服务 | 统一 InitializationView；普通入口隐藏地址，连接其他云端才编辑；旧云离线不阻塞新增云，恢复旧绑定仍验证身份 |
| Web 备用配对码 | 当前界面仍生成旧配对码 | 改为工作区连接指引，不再调用 pairing 写接口；协议入口单独保留 |
| iOS 无入口设置页 | CloudSettingsView 已无调用入口 | 删除该死页面，保留已有自托管会话及二维码的同云校验 |
| 原生无效设置 | multiAgentMode 在原生契约已标 ignored，界面仍宣传可设置 | 服务端明确拒绝，不投影/宣传无效设置；原始原生 schema 保留用于追溯 |
| 历史展示 | 本地历史无 timeline，Web 和预览各自转换 messages，iOS 行为不一致 | Catalog 统一输出 timeline，保留阶段、截断等信息；删除预览和 Web 的重复转换 |
| 单云配置读取 | 状态、恢复、成员、已确认账号各自解析，部分只认 v2 | CloudManager.saved_bindings 统一只读验证；旧配置可用于成员操作，未知版本明确报错，不写空配置覆盖 |
| 当前使用文档 | 混用旧申请/扫码、声称仍有已删除桌面入口、单管理员说明过时 | 更新 README、初始化、多云、配置、控制台及 API 文档；历史验证记录不改写 |

## 保留及退役条件

| 保留内容 | 依据与条件 |
| --- | --- |
| cloud.json v1 迁移、APNs 表结构迁移 | 持久化数据升级边界；没有全量存量数据证明已迁移，不删除恢复路径 |
| consoleToken、无 members 的旧设备 owner 归属 | 既有授权配置语义；设置账号密码会清除旧 Token。退役前需要生产配置盘点和迁移/恢复验证 |
| cloud connect/pair/link-status、HTTP streams、/messages | 仍公开的外部集成契约，不能仅凭当前 UI 不调用便断定无消费者。退役需确认调用方、最低版本并更新契约 |
| messages 返回字段及增量解码 | 仍公开的响应格式；当前 UI 已统一使用 timeline，外部消费者尚未盘点，不在本次移除传输契约 |
| 原生 turns / canonical 两种输入 | app-server 和 rollout 当前仍产生 turns，并非失效兼容；内部 turns 也是任务完成追踪依据 |
| 首包全量、revision gap 重同步、断线恢复 | 当前增量传输协议的恢复机制，不是可删除的旧实现 |
| 历史审查和部署记录中的旧名称/命令 | 历史事实，不作为当前操作指引；不改写已有证据 |

## 验证与独立审查

- Python 全量 437 项通过，跳过 1 项 APNs provider 测试（当前解释器缺少可选依赖）。随后新增离线旧云场景，当前流程 8 项专项全部通过。
- JS 传输回归 32 项通过；Swift 核心 78 项通过；macOS Swift 类型检查通过；iOS 完整模拟器构建成功。
- `tests/check_connection_workflow.cjs` 验证真实渲染的 Web 桌面/移动指引：默认云、自托管云、正确命令、没有旧配对写请求。HTTP/API 为隔离夹具。
- `tests/check_independent_workspace_ui.cjs` 通过，覆盖独立工作区创建、登录门禁和操作能力。
- GPT-6 Astra 独立审查发现旧云离线阻塞新云、自托管指引错指默认云两项 P2，修复后复审接受；审查者独立重跑 Python 当前流程 8 项和 JS 客户端 14 项。
- Python 编译、相关 JS 语法、最终差异检查通过。

复现关键检查：

```sh
python3 -m unittest discover -s tests -q
node --test tests/test_client.js tests/test_console_client.js tests/test_cloud_settings.js tests/test_notification_client.js
swift test --package-path iOS
xcrun swiftc -typecheck -parse-as-library -swift-version 5 -target arm64-apple-macos13.0 desktop/*.swift
# 使用已安装的 playwright；另开终端在仓库根目录启动只读静态服务：
python3 -m http.server 8923 --bind 127.0.0.1
node tests/check_connection_workflow.cjs
```

**已知验证限制：** 较早的 `check_mobile_ui.cjs` 在工具菜单与设计稿位置比较处失败：实际 y=399.203125，旧预期 y=503.203125。用本次修改前的暂存资源在独立临时目录运行，同一断言、同一坐标失败；已确认不是本次引入。没有擅自改动现有菜单或放宽该断言。真实手机、桌面窗口点击和生产 HTTPS 连接尚未验收；构建/夹具测试不代表已安装或部署。

## 恢复

### 二维码取消与扫码入口补充

桌面二维码页的「取消」通过独立轮询凭证作废待扫码邀请，返回权限设置并保留选项；已确认绑定不可撤销。云端取消与扫码确认共用锁，取消结果同步父目录，落盘失败按可见文件恢复；本地保存失败保留进度供重试。取消中的旧轮询不再更新界面。iOS「我的」左上角新增扫一扫入口，直接复用 `WorkspaceBindingView` 的相机、权限确认与绑定流程。

取消变更按 R2 审查：54 项初始化/绑定/恢复测试通过，桌面 Swift 类型检查通过。GPT-6 Astra 独立审查提出的轮询竞争和目录持久性问题均已修复，复审接受；审查者另验证 20 次取消/确认并发，均仅一方成功。iOS 扫码入口按 R1 验证，完整 arm64 模拟器构建通过；尚未执行真实相机扫码或桌面窗口点击验收。

发布时需先更新云端取消接口，再更新桌面端；旧云不支持取消时保留待扫码进度并报告失败，不假装已作废。回退仅撤回界面入口及对应调用，保留云端对 cancelled 状态的拒绝直到旧邀请过期，避免重新激活已取消二维码。本次未安装或部署。

本次没有删除账号、设备、凭证或更改数据格式。发布前分别保留工作区私有状态、云端账号和设备授权数据；代码回退只撤销本次差异，不覆盖已有暂存内容或恢复旧授权文件。新云绑定复用现有安装/恢复机制，未知结果不重新签发或重放任务。兼容接口的进一步退役必须先完成消费者与存量数据盘点。
