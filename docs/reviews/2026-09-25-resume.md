# 暂停后继续执行

范围与风险：R2，修改 desktop follower 的 resume 投递和 iOS 按钮标签；不改变公开请求字段、编辑消息操作、权限或存储格式。目标是暂停后继续当前上下文，保留暂停消息、附件和已产生的执行记录。原有 README.md、carryon/app_server_bridge.py、docs/QUICKSTART.md、tests/test_app_server_pagination.py 改动不属于本次任务。

问题证据：旧 resume 映射 thread-follower-edit-last-user-turn v2。原生编辑处理器先 thread/revert 或 thread/rollback，再重新启动；它的历史删除语义不符合继续执行。用户所报“执行上一条”未在真实任务上重现，不能仅凭源码断言其具体内部原因。

当前契约（替代 2026-09-12 暂停审查中的 resume 重放契约）：只有最新轮次 interrupted、原生状态 idle 且没有待处理请求时可继续；使用 thread-follower-start-turn v2，request 为当前 threadId 和空 input，context.inheritThreadSettings 为 true。历史和附件保留在原生上下文，不重发用户输入，不调用编辑、回退或回滚。空输入续跑再次暂停后仍可继续。投递前再次检查当前轮次，请求日志继续约束重复请求和 uncertain 状态。

原生依据：本机 ChatGPT.app 26.915.31945（9922）app.asar 内 webview/assets/app-initial-a498f911edeb.js：eJt 是编辑回退路径；CXt 是 startEmptyTurn 实现，默认 continuationInput 为 []；应用更新后的原生恢复也调用 startEmptyTurn({continuationInput:[]})；$Xt 的 inheritThreadSettings 默认 true。所读 JS 的 SHA256 为 34a75db63c7137eb4caecdba1f36d631c10c7912487e532fd5e9dafb175bb9be。只读检查，未修改 App 包。

验证包括多轮历史及部分结果保留、附件上下文不重发、canonical 历史、空输入再次暂停、旧轮次拒绝、发送前轮次变化拒绝、重复请求只发送一次，以及超时后阻止再次投递。模拟 IPC 验证不代表真实模型的续跑结果。

已验证：
- `python3 -m unittest discover -s tests`：502 项，501 通过、1 跳过；输出有 SQLite 连接 ResourceWarning 和 ZIP 重复条目测试警告，无测试失败。
- `python3 -m unittest tests.test_operations tests.test_bridge`：34 项通过。
- `swift test --package-path iOS`：Swift Testing 104 项及 XCTest 4 项通过。
- `node --test tests/test_client.js tests/test_console_client.js tests/test_cloud_settings.js tests/test_notification_client.js`：35 项通过。
- `sh iOS/scripts/compile-device.sh`：arm64 iphoneos 无签名构建通过；检查生成的 CarryOn.app 可执行文件存在且包含“继续执行”标签。构建报告 ConversationPhotoPicker.swift 已有 actor 隔离警告，与本次标签修改无关。
- Python compileall、项目文档及 CI 所列相关 JavaScript/shell 语法检查、最终 `git diff --check` 通过。

恢复方案：只撤销本次 operations.py、ChatAppearance.swift、test_operations.py 的定向差异及本审查文档；不回退整个工作区、不改日志和用户数据。重新启用旧 resume 会恢复已知历史回退风险，因此部署回退时应停止使用继续按钮，待修正后再验收。当前未安装、重启服务、提交或部署。

人工验收：在可控测试任务先发送 A 并完成，再发送不同的 B，执行中暂停，再继续，确认 A、B 及 B 的部分结果仍在且继续 B；再暂停、继续一次；另测带附件的 B。需运行修复后的服务。真实手机和原生模型端到端尚未验证。

独立审查：GPT-5.6 Luna 基于改动、原始原生 JS 和测试形成“有条件接受”结论，未发现代码阻断问题；独立运行 operations 19 项和全量 Python 502 项（1 跳过）均通过。最终接受仍需任务发起者完成上述真实端到端人工验收。app-server backend 本就不提供 resume，本次仍只覆盖 desktop follower。
