# iOS 视觉统一（2026-09-20）

范围：参考 ChatGPT iOS 的简洁黑白灰层级，对 CarryOn iOS 公共样式、页面和输入区进行统一。不是官方界面的像素复刻。

## 实现边界

- 统一系统字体、语义色、卡片与控件圆角、列表间距和图标风格。
- 登录、项目/会话列表、动态、设置、工作区与请求表单复用公共样式；系统弹层保留原生展示。
- 输入区变为同一容器内的正文与工具行，保留附件、模型、发送、停止、重启和排队的原条件及回调。
- 跟随系统浅色/深色外观，代码高亮同步主题；品牌单色图标使用模板渲染。
- 未改动网络请求、权限、状态规则、路由、功能入口或文案。此前会话创建、文字选择和导航修复属于独立的已有改动。

## 验证

- iOS Simulator Debug 完整生产入口构建通过。
- Swift Core 97 项测试通过。
- 现有消息导航回归通过：早期消息跳转成功，跳转目标消费成功。
- 隔离模拟器使用生产视图/组件生成浅色和深色共 16 张截图，检查登录、设置、工作区切换、连接说明、会话行、Markdown/代码、三种输入区状态和问题表单。模拟数据不连接真实账户，不发送真实消息。
- 截图发现深色品牌图标对比度问题，已改为模板颜色并重新渲染检查。
- 本地证据：`.runtime/style-gallery/`（截图、临时画廊入口、构建日志）；`.runtime/navigation-regression/result.json`；`/tmp/carryon-style-swift-tests.log`。
- 画廊中的会话行和消息为真实组件组合，不是完整的真实服务会话。未逐项操作所有弹层，也未验证真机、相机和服务端流程；本次不宣称这些功能完成端到端验收。
- 未提交、部署或安装到用户真机。
