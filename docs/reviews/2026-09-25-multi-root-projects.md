# 多根目录项目归组修复

## 范围与授权

用户授权修复，明确禁止重启服务。变更仅在隔离工作树；不改原工作区未提交内容，不提交、推送、部署或操作真实会话。

风险 R2：调整设备 API 的项目身份及创建会话的目标解析。原生 Codex 项目 ID 是归属权威；目录和原生名称是展示元数据。列表、详情、通知和创建结果核对使用同一身份规则。

## 行为与影响

- 原生项目按带命名空间的原生 ID 哈希分组，保留名称和全部根目录；不同项目共享首根目录也保持独立。
- 无明确归属时，只有目录或 Git common-dir 唯一对应原生项目才推断归属；有歧义时保留路径分组。显式无项目标记继续有效，数据库原生项目 ID 优先。
- 搜索覆盖原生名称及全部根目录。保留原有无会话不展示、可用会话过滤及独立工作区“最近”行为。
- 旧客户端显示缓存可能保留目录 ID；仅创建入口接受能唯一定位原生项目的旧 ID，共享目录拒绝。列表始终返回新 ID。所有受支持客户端缓存均淘汰旧 ID 后可移除此入口；本次不改持久化缓存或重放未确认请求。
- 旧项目详情 ID 不做重定向；更新后需返回项目列表重新选择。原生项目与通知数据不迁移、不写回。

## 验证

- 针对性 Catalog、Workspace、创建及独立工作区回归；覆盖多根、共享根、共享 Git 仓库的歧义、子目录、搜索、详情、通知、改名/根目录重排稳定性及旧 ID 兼容。
- 完整 Python suite：498 tests，OK，1 skipped（之后补充共享 Git 仓库及空独立工作区测试，并重跑相关套件，最终 63 tests 全部通过）。
- CI 指定的四个 JavaScript suite：35 passed；JavaScript、shell 语法及 Python 编译检查通过。
- 使用已有 build 1.4.0 / setuptools 84.0.0 环境构建 wheel；核对 wheel 内三个修改模块与源码一致。
- 真实 Codex 数据只读输入临时 Workspace：saas平台 12、DMC产品矩阵 2、CarryOn 2；列表与详情计数相同，根目录数分别为 2、3、1。
- 未进行真机、真实新建会话或部署后验证；当前运行服务不会加载隔离工作树修复。

## 审查与恢复

独立审查发现共享 Git common-dir 的多项目归属歧义，已改为汇总全部候选项目并增加负向测试；独立复核已通过，无剩余阻断问题。用户仍需人工验收，发布另行授权。

本次未影响运行服务，无运行态恢复步骤。未来发布若回退，恢复旧程序并让客户端刷新项目列表；保留 Codex 数据及 Journal，禁止清空或重发 uncertain 请求。项目 ID 是读时投影，没有数据库迁移。
