# 2026-09-15 本地更新审查

## 范围与授权

用户授权更新本地服务、桌面端、iOS App，并提交推送当前代码。基线为 df36555，候选为其上的绑定恢复、工作区登记管理、CODEX_HOME 默认路径及 iOS 额度与项目卡片展示改动。设计探索产物和探测脚本不属于此次交付。推送当前 codex/carryon-rename 分支；云端 main 部署不在本次更新范围。

## 审查结论

接受本地更新。发布执行者未参与这些候选业务实现，直接审阅原始差异、绑定与工作区契约、调用入口和测试，可拒绝发布。现有基线的原实施审查见本目录其他报告；本次没有修改候选业务实现。

设备凭证有效性由云端判定；明确凭证失效才允许恢复，网络失败保持未知。替换核对目标云端、绑定 ID 与原凭证指纹，保存已领取结果，失败可恢复且不覆盖其他云端。工作区删除仅注销登记，运行实例先停止，保留数据文件。桌面与 CLI 使用相同服务；iOS 额度展示消费共享账号额度接口。未发现阻断本地更新的缺陷。

## 验证

- Python 全量 397 项通过，1 项既有跳过。
- Node 31 项通过；Python 编译与差异空白检查通过。
- Swift Testing 73 项通过，另有 XCTest 4 项通过。
- arm64 macOS 完整安装包构建、签名校验通过；独立 CLI 8 项回归通过。
- 桌面打包冒烟验证多工作区发现、切换、隔离、启停、登记移除与文件保留。
- iOS 开发签名构建成功，并核对嵌入 GiphyUISDK。安装和运行结果单独记录在本地交付日志。

## 更新与恢复

默认工作区更新前未运行。完整私有目录和旧桌面应用保存在 .runtime/update-20260915/backup；旧 CLI 入口记录于同目录 rollback.json，新 CLI 使用独立版本目录。启动或验证失败时恢复旧应用与 CLI 入口，保留全部用户数据。实际启动后已验证 Codex、云端连接与项目读取，token、cloud、standby、onboarding 文件哈希一致。

三个遗留测试进程仍占用 8771、8772、8892，但其数据目录已无认证 Token 与 service.json，未对这些无法认证的实例执行替换或停止。默认服务使用 8769。

人类产品验收仍需检查真实手机界面、交互和扫码恢复；测试与安装不能替代该验收。无生产数据迁移或系统配置变更。
