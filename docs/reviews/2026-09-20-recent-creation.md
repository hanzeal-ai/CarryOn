# 新建任务的空闲控制会话选择

范围：为指定项目创建任务时，优先“最近”无项目分组，再查其他会话；各组按更新时间倒序。目标项目独立于借用的控制会话。R2：改变控制会话选择与桌面加载流程，保留创建授权、原生实时状态和发送前校验。

影响：`creation.py` 选择候选；`ipc.py` 通过已验证的 `codex://threads/<UUID>` 桌面链接加载未加载会话，重新读取 owner/快照；`bridge.py` 将控制会话有效性与目标项目校验分开。空闲判断、审批阻断、未确认请求阻断、发送幂等和创建结果的项目校验仍沿用原有实现。加载可能切换桌面显示的会话，不发送消息；非 macOS 不执行该唤起路径。

验证：

- `python3 -m unittest discover -s tests`：458 项通过，跳过 1 项。
- 随后补充最近组超时后继续其他组的预算修正；最终 `test_project_creation.py` 19 项、`test_ipc.py` 8 项通过，覆盖最近优先、组内时间排序、跨项目借用、唤起成功/失败、唤起后忙碌/未知、审批/未确认请求跳过、目标项目改变、授权撤销及非法 ID。
- 本机原生只读验证：一个“最近”历史会话的 sidebar snapshot 原先超时；调用 `wake_snapshot` 后取得相同会话 ID、`idle`、`resumeState=resumed`，未调用 start 或创建任务。随后恢复当前任务页面。
- `git diff --check` 通过。

未验证：真实发送创建指令至借用会话及创建结果端到端流程；本次未发送真实消息、未创建任务、未部署。手机仍使用已运行的服务版本，源码修改不会自动替换该服务。上线前需人工验收和单独部署授权。

恢复：无迁移、无 Codex 数据库写入。尚未部署；回退本次 creation/bridge/ipc 的变更即可恢复旧选择规则，保留之前的 clientThreadId 解析和 iOS 文字选择变更。若以后发布，回切此前服务包；已记录的请求仍须核对，不自动重发。

独立审查：Luna 审查者基于需求、最终差异及测试形成“有条件接受”结论，未发现阻断代码交付的问题；保留真实创建端到端流程与上线人工验收条件。
